# Frontend (Astro) env (production example)
# Copy to `frontend/.env.local` (or `.env`) for production.

# API base URL used by `frontend/src/lib/publicApi.ts`
PUBLIC_API_BASE_URL=https://your-domain.tld/api
