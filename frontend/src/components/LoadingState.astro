---
interface Props {
  label?: string
}

const label = typeof Astro.props.label === 'string' ? Astro.props.label : 'Loading…'
---

<div class="flex items-center gap-3 text-sm text-foreground/70" role="status" aria-live="polite">
  <span class="w-4 h-4 rounded-full border-2 border-accent/30 border-t-accent animate-spin motion-reduce:animate-none"></span>
  <span class="min-w-0 truncate">{label}</span>
</div>

