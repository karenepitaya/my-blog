# Frontend (Astro) env (example)
# Copy to `frontend/.env.local` (or `.env`) for local development.

# API base URL used by `frontend/src/lib/publicApi.ts`
PUBLIC_API_BASE_URL=http://localhost:3000/api

