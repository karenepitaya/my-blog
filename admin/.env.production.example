# Admin (Vite) env (production example)
# Copy to `admin/.env.local`.
# All public env vars must be prefixed with VITE_.

# API base (recommended in production)
VITE_API_BASE_URL=https://your-domain.tld/api
# Optional: server API base (legacy/compat fallback)
VITE_SERVER_API_BASE_URL=https://your-domain.tld/api

# Optional: Gemini API key for AI assistant
VITE_GEMINI_API_KEY=

# Self-hosted Chinese font CSS (Noto Sans SC) - used by admin/styles/fonts.ts
VITE_FONT_CSS_ZH=https://your-domain.tld/fonts/current/noto-sans-sc/fonts.css

# Font asset origin for resolving url(/fonts/...) inside fonts.css
VITE_FONT_ORIGIN=https://your-domain.tld

# English mono font CSS (JetBrains Mono). Default uses Google Fonts for verification.
VITE_FONT_CSS_EN=https://fonts.googleapis.com/css2?family=JetBrains+Mono:wght@400;600;700&display=swap

# Optional: analytics mocks (true/false)
VITE_ANALYTICS_MOCK=false

# Optional: config diagnostics mock (true/false)
VITE_CONFIG_DIAGNOSTICS_MOCK=false

# Optional: enable dev preview in non-dev builds (true/false)
VITE_ENABLE_DEV_PREVIEW=false
